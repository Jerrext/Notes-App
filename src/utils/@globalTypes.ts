export enum InputTypes {
  INPUT = 'input',
  TEXTAREA = 'textarea',
  MULTISELECT = 'multiselect',
}

export enum ButtonTypes {
  DEFAULT = 'default',
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
}
