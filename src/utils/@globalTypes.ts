export enum ButtonTypes {
  DEFAULT = 'default',
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
}

export enum ModalWindowTypes {
  CREATE_NOTE = 'createNote',
  NOTE_INFO = 'noteInfo',
  EDIT_NOTE = 'editNote',
}
